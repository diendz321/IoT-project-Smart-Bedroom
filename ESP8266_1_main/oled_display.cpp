#include "oled_display.h"
#include "config.h"
// Them thu vien (vi du: Adafruit_SSD1306.h)

void setupOLED() {
  // Viet code setup thu vien SSD1306
}

void updateOLED(float temperature, String currentTime) {
  // Viet code de xoa man hinh
  // Format va hien thi gia tri nhiet do
  // Format va hien thi thoi gian (currentTime)
  // display.display();
}